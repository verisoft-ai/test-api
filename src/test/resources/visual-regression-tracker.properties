# Properties file for visual regression tracker build

# api url localhost:4200 is fixed
api.url = http://localhost:4200
api.key = api.key
# VRT project name
project.name = project name
# the current branch name you want to run and compare the screens
branch.name = branch.name
# VRT build ID
ci.build.id = build.id

